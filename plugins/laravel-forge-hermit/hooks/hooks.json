{
  "description": "Forge write guardrails.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bun",
            "args": ["${CLAUDE_PLUGIN_ROOT}/hooks/write-confirm-gate.ts"],
            "timeout": 5
          }
        ]
      }
    ]
  }
}

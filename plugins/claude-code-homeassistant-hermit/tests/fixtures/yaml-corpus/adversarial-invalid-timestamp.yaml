# Looks like a timestamp to PyYAML's resolver but the constructor raises
# ValueError (month 13) -- yaml.safe_load REJECTS the whole document.
# Bun.YAML happily returns the string "2024-13-45".
not_a_date: 2024-13-45

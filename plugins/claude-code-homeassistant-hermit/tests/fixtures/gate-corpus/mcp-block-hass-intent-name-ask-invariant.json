{
  "gate": "mcp",
  "stdin": "{\"tool_name\":\"mcp__homeassistant__HassTurnOff\",\"tool_input\":{\"name\":\"front gate\"}}",
  "config_mode": "ask",
  "verdict": "block",
  "note": "Hass* intent tool targets by name (HA fans it out server-side), so it hard-blocks in EVERY mode incl. ask — the old Hass.* matcher hard-blocked these and the widened matcher must keep that guarantee. Byte-identical to the Python no-id block; only bare-named script tools are mode-dependent."
}

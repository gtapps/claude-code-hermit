{
  "gate": "mcp",
  "stdin": "{\"tool_name\":\"mcp__homeassistant__GetDateTime\",\"tool_input\":{}}",
  "verdict": "allow",
  "divergence": "Read-only tool allowlist added post-port: GetDateTime carries no entity_id and never actuates, so the widened matcher allowlists it (exit 0). The retired Python gate never saw it (old matcher was Hass.*) and would fail closed on the empty id list (exit 2).",
  "expect_py": { "exit": 2 }
}

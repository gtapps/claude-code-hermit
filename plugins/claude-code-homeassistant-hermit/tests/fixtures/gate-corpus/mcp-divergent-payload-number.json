{
  "gate": "mcp",
  "stdin": "42",
  "verdict": "block",
  "divergence": "Python: uncaught AttributeError (int.get) -> exit 1 fail-open. TS blocks (exit 2).",
  "expect_py": {
    "exit": 1,
    "stderr_contains": "AttributeError"
  }
}

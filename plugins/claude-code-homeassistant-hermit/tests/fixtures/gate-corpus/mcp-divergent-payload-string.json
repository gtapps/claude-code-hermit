{
  "gate": "mcp",
  "stdin": "\"lock.front_door\"",
  "verdict": "block",
  "divergence": "Python: uncaught AttributeError (str.get) -> exit 1 fail-open. TS blocks (exit 2).",
  "expect_py": {
    "exit": 1,
    "stderr_contains": "AttributeError"
  }
}

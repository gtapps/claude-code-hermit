
# claude-code-hermit (project — hermit state tracked in git)
# Only .env files are excluded — everything else is versioned.
# Run /claude-code-hermit:migrate to scan for unprotected credentials.
.env
.env.*
